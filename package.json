{
  "name": "color-filter",
  "version": "1.0.0",
  "description": "apply color filter for people with color blindness",
  "main": "index.js",
  "scripts": {
    "test": "npm run build && node ./test/_tester.js",
    "scss": "node-sass --watch src/scss -o src/css",
    "build": "rm -rf ./dist/ && mkdir ./dist/ && cat ./src/scss/* > dist/_index.scss"
  },
  "author": "raj",
  "license": "ISC",
  "dependencies": {
    "cfonts": "^1.1.3",
    "fs": "0.0.1-security",
    "node-sass": "^4.7.2"
  }
}
